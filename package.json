{
    "name": "@entgldb/root",
    "private": true,
    "version": "0.1.0",
    "description": "EntglDb - JavaScript/TypeScript Implementation",
    "repository": {
        "type": "git",
        "url": "https://github.com/entgldb/entgldb-js"
    },
    "scripts": {
        "build": "pnpm -r build",
        "test": "pnpm -r test",
        "lint": "pnpm -r lint",
        "clean": "pnpm -r clean",
        "generate:proto": "pnpm --filter @entgldb/protocol generate"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "typescript": "^5.3.3",
        "prettier": "^3.1.1",
        "eslint": "^8.56.0"
    },
    "engines": {
        "node": ">=18.0.0",
        "pnpm": ">=8.0.0"
    }
}